#include <stdio.h>
#include <conio.h>

void main();
{
    int a,b,c,average;
    printf("enter three integer numbers:");
    scanf("%d%d",&a,&b,&c);
    sum=a+b+c;
    average=sum/3;
    printf("average:%d",average);
    return 0;
}
